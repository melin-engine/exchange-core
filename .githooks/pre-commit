#!/usr/bin/env bash
# Pre-commit hook: ensure all Rust code is formatted.

if ! cargo fmt --check 2>/dev/null; then
    echo "error: code is not formatted. Run 'cargo fmt' and try again."
    exit 1
fi
